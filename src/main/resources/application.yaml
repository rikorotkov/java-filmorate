spring:
  application:
    name: filmorate

  datasource:
    url: jdbc:h2:file:./db/filmorate
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always

server:
  port: 8080

logging:
  level:
    org.zalando.logbook: TRACE